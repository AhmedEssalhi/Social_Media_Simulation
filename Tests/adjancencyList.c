#include <stdio.h>
#include <stdlib.h>
#define MAX 10

typedef struct adjNode adjNode;
struct adjNode {
	int data;
	adjNode *next;
};

struct adjList {
	adjNode *head;
};

struct Graph {


int main()
{
	return 0;
}
